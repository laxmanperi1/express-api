apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - servicemonitor-express-api.yaml
  - prometheus-rules.yaml
  - grafana-dashboard.yaml
  - grafana-admin-secret.yaml

commonAnnotations:
  argocd.argoproj.io/sync-wave: "4"
